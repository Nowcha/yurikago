# 受け入れテスト手順書（初回セットアップ後）

前提: `npm install` 済み、`src/lib/store.ts` の firebaseConfig 貼り替え済み、firestore.rules デプロイ済み。
所要目安: 30〜40分。夫のPC＋妻のスマホ（または2つのブラウザプロファイル）で実施。

## A. 自動テスト
- [x] `npm test` → deadline計算・データ整合性・overview集計ロジック 全ケースPASS（tests/deadline.test.ts, tests/data-integrity.test.ts, tests/overview.test.ts）
- [ ] `npm run test:rules` → ルール 全ケースPASS（Java必要。失敗時はEmulator未起動を疑う）
- [x] `npm run build` → 型エラーなくビルド完了

以下、各項目に **[自動]**（tests/data-integrity.test.ts等で検証済み・再確認不要）と
**[実機]**（Google認証・複数端末同期・実デバイス操作が本質のため自動化不可）を付記。

## B. セットアップ・世帯
- [ ] **[実機]** `npm run dev` → Googleログインできる
- [ ] **[実機]** 予定日＋プロファイル2項目を設定し世帯作成 → タスクが自動生成される
- [ ] **[実機]** プロファイルのチェックを外して作った場合、該当手続き（出産手当金等）が「対象外」で生成される（別世帯で確認→テスト後削除）
- [ ] **[実機・重要]** もう一方の端末でログイン → 参加待ち画面に自分のUIDが表示される
- [ ] **[実機]** 先の端末の「設定 > 世帯 > パートナーを追加」でUID登録 → 相手の画面が数秒で世帯表示に切り替わる
- [ ] **[実機]** 第三のGoogleアカウントでログインしても世帯が見えない（ルール検証。ロジックはtest:rulesで論理検証済みだが実機でも確認）

## C. タスク（夫婦同期の本丸）
- [ ] **[実機]** 一方がタスクを「完了」→ 他方の画面に即時反映される
- [ ] **[実機]** 担当を割り当てるとバッジに表示名が出る
- [x] **[自動]** 出生届の詳細に準備サブタスク4件が出る（data-integrity.test.ts）／[実機] チェックの同期は確認要
- [ ] **[実機]** 「生まれたら期限が決まるもの」セクションに産後手続きが期限なしで並ぶ
- [ ] **[実機]** 手動タスクを期限付きで追加 → タイムラインの正しい位置に入る → 削除できる
- [ ] **[実機]** 公式リンク（江東区・018サポート等）が開ける

## D. 出生イベント（テスト用データで実施）
※本番世帯では出産まで実行しないこと。確認する場合は世帯削除→バックアップ復元を使う
- [ ] **[実機]** 設定の「生まれたら」で出生日登録（2段階確認あり）
- [x] **[自動]** 出生届の期限が「出生日+13日」、児童手当が「出生日+15日」になっている（data-integrity.test.ts で procedure-master.json 実データに対して検証済み）
- [ ] **[実機]** ダッシュボードが「生後◯日」表示に切り替わる

## E. 準備品
- [ ] **[実機]** チェックで実費入力欄が出る → 予算/実績の集計に反映
- [ ] **[実機]** 「不要」で予算集計から除外される
- [x] **[自動]** 「産後に様子見て」対象品目（抱っこ紐・ベビーカー・搾乳器）にwaitUntilBornが設定済み（data-integrity.test.ts）／[実機] バッジ表示は確認要

## F. データ管理・PWA
- [ ] **[実機]** JSONバックアップ → 世帯削除 → 新規世帯作成 → 復元、で内容が戻る（ペイロード構造自体はbuildExportPayloadとしてdata-integrity.test.tsで検証済み）
- [x] **[自動]** ICSの内容（法定期限のみ・未完了のみ・3日前アラーム・DTSTART形式）は data-integrity.test.ts で検証済み
- [ ] **[実機]** ICSファイルをスマホのカレンダーアプリに実際に取り込めるか
- [ ] **[実機]** GitHub Pagesデプロイ後: スマホで「ホーム画面に追加」→ アイコン表示・スタンドアロン起動
- [ ] **[実機]** 一度読み込んだ後に機内モード → アプリが起動し、タスクの閲覧・編集ができる → 復帰後に同期される
  （注: SWはstale-while-revalidate方式のため、アプリの更新は「次回起動時」に反映される仕様）

## G. 週次情報（Phase 1.5）
- [x] **[自動]** weekly-info.jsonが4〜41週まで欠番なく揃い、各週にbabySize/babyNote/momNoteが存在する（data-integrity.test.ts）
- [ ] **[実機]** ダッシュボードに現在の週の「今週の赤ちゃん」カードが表示される
- [ ] **[実機・人力レビュー]** 記載内容を母子手帳・産院資料と突合してレビュー（医学的正確性は自動化不可）

## H. 育児記録（出生日登録後）
- [ ] **[実機]** 出生日登録後、タブ先頭に「きろく」が出現する
- [ ] **[実機]** 母乳左をタップ → 即記録され「前回の授乳から」が0分にリセット
- [ ] **[実機]** ミルクをタップ → 量入力シート → 記録一覧にml表示
- [ ] **[実機]** 「ねた」→ボタンが「おきた」に変わる（睡眠状態のトグル）
- [ ] **[実機]** 一方の端末の記録が他方に即時反映される
- [ ] **[実機・最重要]** 機内モードで記録 → 復帰後に同期される（深夜の産院を想定した最重要ケース）
- [ ] **[実機]** JSONバックアップにrecordsが含まれる（ペイロード構造自体はbuildExportPayloadとして検証済み）
